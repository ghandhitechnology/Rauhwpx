# HWPJSON clipboard fixtures

`clipboard-model.json` is a minimized Hangul clipboard document-model fixture
for `hwpjson_to_hwpx_parts`. Automated conversion tests load this file; it does
not embed a full source document or binary assets.
