[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node server.mjs"
healthcheckPath = "/healthz"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[variables]
RAU_CREDITS_DATA = "/data"
