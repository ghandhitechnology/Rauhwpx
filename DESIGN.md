# Design

<!-- impeccable:design-schema 1 -->

rhwp-studio 의 시각 시스템. 문서에 적힌 값은 모두 코드에 실제로 있는
것이다. 색을 바꿀 일이 생기면 `--n-*` 램프를 고치고, 개별 화면의
CSS 를 직접 만지지 않는다.

## World

**Word for Mac 계열의 현대화된 문서 프로그램 크롬 위에 놓인,
문서 우선(document-first) 에이전트 작업 공간.**

거부하는 배치: 세로 그라디언트 툴바, `#c0c0c0` 실선으로 나눈 패널,
맨 textarea 옆에 붙은 채움 버튼, 양쪽 말풍선 채팅, 문서를 지우고 기술
로그·빈 검토 칸을 같은 비중으로 늘어놓는 3열 오퍼레이터 콘솔.

면은 톤으로 나누고 경계는 알파 헤어라인으로 처리한다. 실선을 지우고
남는 자리를 여백과 높이(그림자)가 대신한다. 색은 절제형 — 중성 램프에
액센트 하나. 에이전트 색(Claude/Codex)은 1px 식별선과 상태 점으로만
남고 면을 채우지 않는다.

Mode: **Operate**. 표현보다 스캔 가능성·일관성·익숙한 조작이 앞선다.

## Token architecture

3계층이고, 아래에서 위로만 참조한다.

```
--n-*        중성 램프 (원천, 밝은/어두운 두 벌)
  ↓
--ui-*       앱 의미 계층 (bg / surface / border / text / hover …)
  ↓
--ag-*       에이전트 사이드바 지역 계층
--color-*    구형 alias (기존 CSS 호환)
```

`--ui-*` 는 값을 직접 갖지 않고 전부 `var(--n-*)` 를 가리킨다. 그래서
30여 개 스타일시트가 램프 한 곳을 고치면 함께 따라온다.
`src/styles/base.css` 가 세 계층을 모두 정의한다.

## Surfaces

| 토큰 | 밝은 | 어두운 | 쓰임 |
|---|---|---|---|
| `--n-canvas` | `#f5f5f7` | `#141416` | 앱 바탕 |
| `--n-surface` | `#ffffff` | `#1e1e20` | 패널·대화상자 본체 |
| `--n-surface-raised` | `#f7f7f8` | `#26262a` | 툴바·헤더·상태바 |
| `--n-surface-sunken` | `#f1f1f4` | `#171719` | 코드/결과 블록, 눈금자 여백 |

문서 영역은 앱 테마와 분리된 축이다. 용지(`--doc-paper`)는 두 테마
모두 흰색이고, 책상(`--doc-workspace`)만 `#e9e9ed` / `#131315` 로
앱 톤을 따라간다.

## Borders

실선 hex 가 아니라 알파다. 어떤 면 위에 올라가든 그 면의 톤을 따른다.

- `--n-hairline` — `rgba(0,0,0,.08)` / `rgba(255,255,255,.10)`. 구획선 대부분.
- `--n-hairline-strong` — `rgba(0,0,0,.14)` / `rgba(255,255,255,.17)`. 입력 필드처럼 스스로 형태를 가져야 하는 것.

## Text

밝은 테마 기준 대비율을 괄호에 적었다. muted/placeholder 도 본문
크기로 쓰이므로 4.5:1 을 넘긴다.

| 토큰 | 밝은 | 어두운 |
|---|---|---|
| `--n-text` | `#1d1d1f` (16.6:1) | `#f5f5f7` |
| `--n-text-secondary` | `#545458` (7.4:1) | `#c7c7cc` |
| `--n-text-muted` | `#6e6e73` (5.1:1) | `#a2a2a8` |
| `--n-text-placeholder` | `#757579` (4.6:1) | `#8f8f95` |
| `--n-text-disabled` | `#adadb2` | `#6b6b71` |

`--n-on-accent` 는 채워진 액센트 버튼 위의 잉크다. 밝은 테마는 흰색,
어두운 테마는 `#10203c` — 어두운 쪽 액센트가 밝은 파랑(`#7fa8ff`)이라
흰 글자를 올리면 2.3:1 로 무너지기 때문이다.

## Radius

칩 4 / 입력·버튼 6 / 패널 8. 기존 이름을 유지해 전체가 자동으로 따라온다.

```
--radius-sm: 4px    --radius-md: 6px    --radius-lg: 8px
```

에이전트 작업 공간은 더 오래 머무는 대화·입력 면에 `6 / 10 / 14px`의
느슨한 3단계를 `--ag-r-chip / -field / -panel` 로 쓴다. diff·코드처럼
조밀한 내부 정보는 앱의 `4 / 6 / 8px` 체계를 유지한다. 작업 로그라는
이유로 전체 작업 공간을 더 각지게 만들지 않는다.

## Agent buttons: 손그림 윤곽

에이전트 사이드바와 전체 화면 작업 공간의 버튼은 위의 고른 반경 대신
펜으로 그린 듯한 윤곽을 쓴다. 두 가지 장치다:

1. 모서리마다 미묘하게 다른 `border-radius`(두 축 `a b c d / e f g h`
   형태). 고른 반경은 기계적으로 읽힌다.
2. `--ag-sketch-line` = `url(#ag-sketch-line)` displacement 필터.
   `feTurbulence`(fractalNoise) + `feDisplacementMap` 조합이고, 정의는
   `src/ui/agent-sidebar/index.ts` 가 사이드바 루트에 한 번 심는다.
   `display:none` 으로 숨기면 Safari 가 참조를 무시하므로 0 크기
   `.ag-sketch-defs` 로 둔다.

규칙은 `agent-sidebar.css` 끝의 "손그림 버튼" 절에 모여 있다. 색·호버·
비활성은 기존 규칙을 그대로 쓰고 윤곽만 얹는다 — 필터가 테두리, 채움,
포커스 링을 함께 흔들기 때문이다. 편집기 크롬(도구 모음, 대화상자)과
캘리브레이션 마법사처럼 사이드바 밖의 표면에는 적용하지 않는다.

## Elevation

오프셋 + 부드러운 블러 2겹. 0-오프셋 컬러 헤일로는 쓰지 않는다.

```
--n-elev-1  0 1px 2px  rgba(0,0,0,.04),  0 1px 1px rgba(0,0,0,.03)
--n-elev-2  0 4px 12px rgba(0,0,0,.08),  0 1px 3px rgba(0,0,0,.04)
--n-elev-3  0 12px 32px rgba(0,0,0,.14), 0 2px 6px rgba(0,0,0,.06)
```

`--shadow-light / -dropdown / -dialog` 가 각각 elev-1 / 2 / 3 을 가리킨다.

## Accent

`--accent-primary` `#6182d6` (어두운 테마 `#7fa8ff`). 기본 동작,
선택 상태, 눈금자 마커.

에이전트 식별색은 액센트가 아니다.
`--ag-claude` `#c4785a` / `--ag-codex` `#5b8fd4` 는 리뷰 카드의 1px
왼쪽 선, 입력 필드 포커스 링, 상태 점에만 쓴다. 2px 이상의 컬러 바나
버튼 채움으로 승격하지 않는다.

## Type

시스템 스택(`-apple-system` → `맑은 고딕` → `Segoe UI`). 한국어가
1차 언어라 별도 웹폰트를 싣지 않는다.

10 / 11 / 12 / 13 / 14 / 16px. 본문·컨트롤은 13–14px, 보조 라벨 11–12px,
코드·수치 10–11px(mono). 모노 활자는 펼친 기술 세부 정보와 실제 좌표에만
쓴다. 대화, 스레드 제목, 진행 요약은 UI 산세로 읽힌다.

## Motion

지수형 ease-out 에서 이미 보이는 상태로 안착한다. 바운스·스프링
오버슈트는 쓰지 않는다.

```
--ag-motion-out     cubic-bezier(0.16, 1, 0.3, 1)
--ag-sidebar-ease   cubic-bezier(0.2, .72, 0, 1)      320ms
```

사이드바 transform, 편집 영역 inset, 용지 재정렬 루프는 같은 320ms
계약을 쓴다. 서로 다른 시간축으로 패널과 용지가 따로 움직이지 않는다.

모든 애니메이션은 `prefers-reduced-motion: reduce` 에서 꺼진다.

## Icons

유니코드 글리프를 아이콘으로 쓰지 않는다. 12 그리드 · `currentColor` ·
1.25 스트로크 · 둥근 캡으로 직접 그린다.
`src/ui/chevron.ts`, `src/ui/agent-sidebar/icons.ts`.

예외: 도구 모음의 한국어 기능 아이콘은 제품의 기존 기능 언어를 보존하기
위해 스프라이트(`/images/icon_small_ko.svg`, 어두운 테마는 `_dark` 변형)를
그대로 쓴다. 아이콘을 바꾸는 대신 한 줄 도구 밴드, 단일 행 라벨, 그룹
사이의 여백과 헤어라인으로 더 편안하게 제시한다. 기능은 숨기거나 없애지
않고 좁은 창에서는 가로로 이어진다.

## Agent workspace

`--ag-*` 는 `--n-*` 위에 얹은 지역 계층이다.

- 기본 모드는 문서 + 오른쪽 에이전트 사이드바다. 문서가 항상 주인공이고
  에이전트는 문서를 보조한다. 전체 화면은 별도 제품이 아니라 대화에 집중할
  때만 여는 선택적 `agent focus` 모드다.
- focus 모드는 스레드 레일 + 폭이 제한된 대화의 2영역이 기본이다. 검토는
  대기 중인 변경이 있을 때 배지로 알리고 필요할 때만 오른쪽 drawer 로 연다.
  빈 검토 칸을 위해 화면의 일정 비율을 예약하지 않는다.
- 헤더의 첫 번째 정보는 현재 문서명과 선택 맥락이다. 프로바이더·모델·추론
  강도·권한은 입력과 함께 결정하는 실행 맥락이므로 composer 의 보조 행에 둔다.
- 입력은 하나의 차분한 면이다. `.ag-composer-field` 가 textarea 와 보내기
  버튼을 같은 테두리 안에 담고, 포커스 링은 필드 전체(`:focus-within`)에
  걸린다. composer 의 메타데이터와 도구도 같은 컨테이너에 속한다.
- 사용자 메시지는 가라앉은 블록, assistant 메시지는 열린 산문으로 구분한다.
  꼬리 달린 양쪽 말풍선이나 명령 프롬프트 흉내는 쓰지 않는다.
- 작업 내역은 기본적으로 `작업 완료 · 5단계 · 28초` 같은 한 줄 요약이다.
  도구 이름, 인자, 결과, op 번호는 사용자가 펼쳤을 때만 보이는 감사(audit)
  정보이며 대화의 시각적 기준선이 아니다.
- 검토 위치는 사람이 읽을 수 있는 문서명·쪽·문단 맥락을 먼저 보여 주고,
  `§1 ¶42 c0–18` 같은 내부 좌표는 보조 정보로 남긴다. 변경 항목은 앞으로
  문서의 해당 위치로 이동하고 강조할 수 있어야 한다.
- 스레드 레일은 번호가 붙은 로그가 아니라 문서와 작업을 찾는 탐색 영역이다.
  실제 문서별 저장 관계가 생기기 전에는 시각적으로 문서별 그룹인 척하지 않는다.
- 권한 상태와 스킬 진입은 모델 설정의 일부가 아니다. composer 의 도구 행에
  항상 보이게 두어, 설정 패널을 열지 않아도 접근할 수 있다.
- `바로 실행 / 계획`은 권한과 독립된 작업 방식이다. composer 도구 영역의
  첫 행, 권한 위에 작은 세그먼트로 두고 계획 모드에서만
  `계획 중 / 승인 대기 / 전환 중 / 실행 중` 상태를 헤어라인 칩으로 표시한다.
- 구현 계획은 말풍선이 아니라 기존 편집 검토 영역의 운영 카드로 고정한다.
  단계·예상 파일·검증·위험을 먼저 보여 주고, 긴 가정·결정·제외 항목은
  접근 가능한 disclosure 뒤에 둔다. 실행 권한은 `승인하고 실행` 버튼의 정확한
  계획 ID로만 넘어가며 일반 채팅 답변은 계획 수정 의견으로 취급한다.
- 다른 탭이 에이전트 연결을 차지하면 상태만 알리지 않고 `이 탭에서 연결`
  동작을 함께 제공한다.

## Empty document

문서가 없을 때 편집 책상 중앙에 파일 열기 동작과 드롭 위치만 표시한다.
튜토리얼, 사용 예시, 안심 문구는 넣지 않는다. 문서를 열면 이 면은 즉시
사라지고 용지가 같은 자리를 차지한다.

## Known gaps

- `#editor-area` 의 `transition: margin-right` 는 레이아웃 애니메이션이지만
  의도된 것이다 — ResizeObserver → viewport-resize 경로가 용지 가운데
  정렬을 갱신하는 데 이 값에 의존한다.
- agent focus 모드는 아직 문서를 덮는다. 장기적으로는 문서 preview/split 를
  열어 대화와 변경 위치를 동시에 확인할 수 있어야 한다.
- 채팅 스레드는 현재 `localStorage` 전역 목록이며 문서 ID 에 연결되어 있지
  않다. 문서별 탐색·동기화·감사 이력은 데이터 모델 변경 뒤에 제공한다.
