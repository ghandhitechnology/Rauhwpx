{
  "name": "@rauhwpx/cloud",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "dependencies": {
    "puppeteer-core": "25.3.0"
  },
  "scripts": {
    "start": "node src/main.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/*.mjs document-runtime/*.mjs worker/*.mjs install/*.mjs",
    "smoke:studio": "node document-runtime/smoke.mjs"
  }
}
